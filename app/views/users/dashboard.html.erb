<div class="container dashboard-container">
  <h1 class="about-color text-center profile-title">HELLO, <%=current_user.first_name.upcase%>!</h1>
  <div class="profile-banner-content">
    <div class="row">
      <div class="col-xs-12 col-sm-8 col-sm-offset-2">
        <div>
          <ul class="tab-base profile-tabs" role="tablist">
            <li role="presentation" class="atab active profile-tabs"><a href="#resource" aria-controls="resource" role="tab" data-toggle="tab">SAVED RESOURCES</a></li>
            <li role="presentation" class="atab profile-tabs"><a href="#event" aria-controls="event" role="tab" data-toggle="tab">SAVED EVENTS</a></li>
          </ul>


          <div class="tab-content">
            <div role="tabpanel" class="tab-pane active" id="resource">
              <% if @saved_resources.empty? %>
                  <h4 class="text-center text-profile"> No saved resources yet!</h4>
                  <p class="text-center photo-profile-logo"> <%= cl_image_tag("venue_xn5fho") %> </p>
                <% else %>
                <% @saved_resources.each do |sr| %>
                  <%= render 'resources/resource_card', resource: sr.resource %>
                <% end %>
              <% end %>
            </div>

            <div role="tabpanel" class="tab-pane" id="event">
              <% if @saved_events.empty? %>
                <h4 class="text-center text-profile"> No saved events yet!</h4>
                <p class="text-center photo-profile-logo"> <%= cl_image_tag("book-event_umsrbj") %> </p>
              <% else %>
                <% @saved_events.each do |se| %>
                  <%= render 'shared/card_event', event: se.event %>
                <% end %>
              <% end %>
            </div>
          </div>
        </div>

      </div>
    </div>

  </div>
</div>
