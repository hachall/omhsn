var bookmarkEventButton = document.getElementById('event-card-<%= @event.id %>')
bookmarkEventButton.innerHTML = "<%= j render 'shared/save_event_icon', event: @event, user: @user %>"
$(function () {
  $('[data-toggle="tooltip"]').tooltip()
})
